export const messagepath=(id,date)=>"rooms/"+id+"/room/messages/all/"+date+Math.random()*1000
export const fetchpath=(id)=>"rooms/"+id+"/room/messages/all"
export const memberspath=(id)=>"rooms/"+id+"/room/members/all"
export const memberfetchspath=(id)=>"rooms/"+id+"/room/members"